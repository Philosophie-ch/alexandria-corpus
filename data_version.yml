# Bump version and description before opening a PR to main.

version: "11-alpha.0"
description: "First alpha release with v11 of the bibliography data, which still contains errors and inconsistencies, but lets us test the new data structure and API."

